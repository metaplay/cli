---
# Alias for the install.sh script (in the repo root) to enable publishing via GitHub Pages which
# doesn't have the 60 requests/hour/IP throttling that raw.githubusercontent.com has.
# Use https://metaplay.github.io/cli/install.sh to download install.sh via Pages.
layout: null
---
{%- include_relative ../install.sh -%}
