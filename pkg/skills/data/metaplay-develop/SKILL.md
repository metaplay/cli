---
name: metaplay-develop
description: Use when working on a Metaplay SDK project — implementing, refactoring, reviewing, or debugging game code (server-side logic, Unity client, game configs) or player incidents (URL with `/playerIncidents/` is referenced), running the server locally, setting up a new project or a custom LiveOps Dashboard, triaging player incidents, and upgrading the SDK. Trigger proactively when the user works in a directory containing `metaplay-project.yaml`, or when code references Metaplay SDK markers (e.g. `[ModelAction(...)]`, `[GameConfigEntry]`, `EntityActor`, `MetaplayClient`).
---

Run `metaplay skills get metaplay-develop` to load the payload.
