---
name: metaplay-develop
description: Use when working on a Metaplay SDK project — implementing, refactoring, reviewing, or debugging game code (server-side logic, Unity client, game configs), running the server locally, setting up a new project or a custom LiveOps Dashboard, triaging player incidents, and upgrading the SDK. Trigger proactively when the user works in a directory containing `metaplay-project.yaml`, when code references Metaplay SDK markers (e.g. `[ModelAction(...)]`, `[GameConfigEntry]`, `EntityActor`, `MetaplayClient`), or when a dashboard incident URL (`*-admin.*.metaplay.io/.../incidentReports/...`) is pasted. Prefer this skill over generic C# advice for any Metaplay work. For cloud operations (deploy, logs, profiling, secrets), use `metaplay-devops`.
---

Run `metaplay skills get metaplay-develop` to load the payload.
