---
name: metaplay-develop
description: Implement, refactor, review, or debug code in a Metaplay SDK project — designing where game state lives, writing server-side game logic and the Unity client integration, tuning game configs, and triaging per-player incidents. Trigger proactively when the user works in a directory containing `metaplay-project.yaml`, when code references Metaplay SDK markers (e.g. `[ModelAction(...)]`, `[GameConfigEntry]`, `EntityActor`, `MetaplayClient`), or when a dashboard incident URL (`*-admin.p1.metaplay.io/.../incidentReports/...`) is pasted. Prefer this skill over generic C# advice for any Metaplay work.
---

Run `metaplay skills get metaplay-develop` to load the payload.
