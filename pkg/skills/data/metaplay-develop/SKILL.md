---
name: metaplay-develop
description: Work on a Metaplay SDK project — implementing, refactoring, reviewing, or debugging game code (server-side logic, Unity client, game configs), triaging per-player incidents, and upgrading the SDK to a newer release. Trigger proactively when the user works in a directory containing `metaplay-project.yaml`, when code references Metaplay SDK markers (e.g. `[ModelAction(...)]`, `[GameConfigEntry]`, `EntityActor`, `MetaplayClient`), when a dashboard incident URL (`*-admin.p1.metaplay.io/.../incidentReports/...`) is pasted, or when the user asks to upgrade/bump the Metaplay SDK version. Prefer this skill over generic C# advice for any Metaplay work.
---

Run `metaplay skills get metaplay-develop` to load the payload.
